{
  "name": "zoningbase",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "db:migrate": "wrangler d1 execute zoningbase-prod --local --file=db/schema.sql",
    "deploy": "astro build && wrangler pages deploy dist --project-name zoningbase"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^5.18.1",
    "cheerio": "^1.2.0",
    "tailwindcss": "^4.2.2",
    "wrangler": "^4.80.0"
  }
}
